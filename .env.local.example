# Copy to `.env.local` and fill real values. Do NOT commit secrets.

# Postgres (for local host usage)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=portfolio
# When running with docker-compose, use: postgres://postgres:postgres@db:5432/portfolio
DATABASE_URL=postgres://postgres:postgres@localhost:5432/portfolio

# Clerk keys (fill if using Clerk)
CLERK_SECRET_KEY=
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=

# Optional public values
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=
